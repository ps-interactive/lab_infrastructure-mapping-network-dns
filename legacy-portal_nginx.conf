server {
    listen 80;
    server_name legacy-portal.example.com;

    root /var/www/legacy_portal;
    index index.php index.html;

    location /admin {
        autoindex on;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php7.2-fpm.sock;
    }
}
